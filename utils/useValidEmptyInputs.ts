export function useValidEmptyInputs(inputs: any) {

    for(const el of Object.keys(inputs.value)) {
        console.log(el);
        
    }



    // if (sortingCurrent.value !== value) {

    //     return false;
    // }

    // if (reverseSorting.value) {
    //     reverseSorting.value = false;
    //     callback();
    //     return false;
    // }
}
